# Copy to .env and adjust. .env is gitignored.
#
# Host ports differ from container ports: docker-compose publishes Postgres on 55432
# and Redis on 56379 to avoid colliding with local installs. These values are for
# running the backend on the host against the compose services.

ENVIRONMENT=local
LOG_LEVEL=INFO

DATABASE_URL=postgresql+psycopg://quorum:quorum@localhost:55432/quorum
REDIS_URL=redis://localhost:56379/0

# Phase 3 agent layer. Without it Quorum still reconciles and the API says explicitly
# that explanations are unavailable and why -- it does not quietly return blank ones.
# Get one at https://aistudio.google.com/apikey
# GEMINI_API_KEY=your-key-here

# Fallback trust-gate thresholds, used only for a category with no trust_scores row.
# Live thresholds are per-category rows in Postgres.
DEFAULT_AUTO_APPLY_THRESHOLD=0.90
DEFAULT_REVIEW_THRESHOLD=0.60
DEFAULT_MIN_SAMPLE_SIZE=50

# Phase 6: gates the approval and audit endpoints only (ADR-0028). Demo-grade -- one
# shared token, no users, no roles, no expiry. Unset means those endpoints return 503,
# not that they are open.
# OPERATOR_TOKEN=change-me

# Share of trust-neutral approvals audited anyway. Approvals that could move a gate are
# always audited regardless. Set to 1.0 to audit everything during a demo.
AUDIT_BASELINE_RATE=0.2

# Amounts at or above this always route to a human, whatever a category has earned
# (ADR-0027). INR 2,000.00 in paise.
HIGH_VALUE_REVIEW_THRESHOLD_MINOR=200000
